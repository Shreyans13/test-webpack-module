{
  "name": "jsmodules",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "spago build",
    "build:watch": "spago build -w",
    "start": "run-p build:watch serve",
    "bundle": "webpack --mode=production --config webpack.common.js",
    "serve": "webpack serve --mode=development --config webpack.common.js"
  },
  "author": "Aviansh",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.15.15",
    "npm-run-all": "^4.1.5",
    "purescript": "^0.15.6",
    "webpack": "^5.75.0"
  },
  "devDependencies": {
    "webpack-cli": "^5.0.0",
    "webpack-dev-server": "^4.11.1"
  }
}
